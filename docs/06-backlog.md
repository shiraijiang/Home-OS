# 06 Backlog

这里只放已经明确推迟或尚未确认的内容。Backlog 不等于承诺开发。

## 已明确推迟

- 记一笔 / 吧台（2026-08-11，见 #65）
- 家务 / 家政角
- 大桌
- 穿搭 / 衣架功能化
- 图鉴 / 书堆功能化
- 季节切换相关扩展
- 未来「隐居地」功能
- 房间画风切换功能

## 已预留但当前不启用

### 记一笔

原定入口为吧台（岛台下木段）。第一版收缩为吃饭系统后推迟，家具本体保留在母版里，未来启用时直接变可点，位置不用动。

### 房间扩建

母版保留门、可拆墙一类结构作为未来加房间的余地。第一版不实现扩建功能，见 #71。

### 隐居地入口

物理入口固定为扫地机器人充电口 / 充电座。当前版本只作为正常充电设施存在，不可点、无提示。未来功能启用时再定义进入方式、场景和返回逻辑。

### 可切换画风

当前只完成 Default 方向。`assets/reference/style-reference-alt-01.PNG` 仅作为未来备选画风方向参考。

未来新增画风时：

- 原则上保持家具语义、主要布局和功能坐标一致。
- 每套画风独立维护 style reference、palette 和必要 sprite 规则。
- 不要求第一版实现换肤系统。

## 已结清：母版图与文档的出入

2026-08-11 对照母版逐项核对，五条全部结清。保留记录供以后查。

| # | 旧母版图上是 | 结果 |
|---|---|---|
| 1 | 图例写「冰箱 = 吃 / 家里有什么 / 不出门吃什么」 | 以私人菜谱库为准（#73） |
| 2 | 时间展示例为五段：清晨 / 白天 / 傍晚 / 夜晚 / 深夜 | 图例不算规格，`04-art.md` 的四段为准（#74） |
| 3 | 鱼缸是亮粉与薄荷绿的珊瑚礁造型 | 新版母版已改为白沙 + 圆润景石（#76） |
| 4 | 图例写「植物状态（季节变化）」 | 图例不算规格，室内盆栽第一版固定（#74） |
| 5 | 图例仍列「记一笔」为第一版入口 | 图例不算规格，按 #65 推迟（#74） |

母版已于 2026-08-11 更新为干净版：说明栏移除、室内灯关闭、岛台台面清空、鱼缸重画。构图、镜头、家具位置比例未变（#75）。

## Base 制作：当前母版还差什么

新版 `scene-master-default.PNG` 已经很接近 Base，但按 `04-art.md` 的 Base 定义还有几项要处理。**这些是出 Base 时要拆掉的，不是要改构图。**

仍需从画面移除（属于 Dynamic，见 #42、#43、#45）：

- 扫地机器人本体（左侧墙边）——充电座留下
- 玄关鞋柜里的四双鞋
- 衣架上的衣服
- 门边墙面挂钩上的包与钥匙串
- 衣架下层电子钟的具体时间显示（`12:47`）——设备本体可留

仍需拆到 Environment 层（见 `04-art.md`「Environment / 环境层」）：

- 主景观窗外的具体景色：蓝天、海面、远山、城市、船
- 右侧玻璃结构外的景色

窗框、玻璃、窗帘、百叶属于 Base，留下。


## 尚未确认

### 正式调色板

`assets/palette.json` 已建立，但当前只记录「尚未锁定」状态。Base 母版稳定后再测试并写入正式颜色，不提前拍脑袋定 HEX 和色数。

### 像素处理脚本

决定流程仍为：中值滤波 → 降采样 → 调色板量化。

`tools/pixelize.py` 尚未创建，因为项目还未进入代码执行阶段。等 Base 与正式 palette 确认后再实现，避免先写一个和最终素材不匹配的脚本。

## 已完成，不再作为 Backlog

- `assets/reference/scene-master-default.PNG` 已入库，并于 2026-08-11 更新为干净版（#75）。
- `assets/reference/pixel-style-reference.PNG` 已入库。
- `assets/reference/style-reference-alt-01.PNG` 已入库。
- 冰箱顶固定装饰已确认（#77），规格转入 `04-art.md`。
- 鱼缸造景已确认（#76），规格转入 `04-art.md`。
