# Reference Images

这里存放 Home-OS 美术执行必须读取的参考图片。

## 需要入库的图片

1. `scene-master.*`
   - 当前选定的房间视觉母版。
   - 约束镜头、透视、房间边界、家具比例和主要家具位置。
   - 后续局部修改不得改变这些结构。

2. `pixel-style-reference.*`
   - 已确认“像素粗度和这种差不多”的风格参考图。
   - 只约束像素颗粒尺度、边缘语言和细节密度。
   - 不允许用它重新设计母版构图。

## 当前状态

图片文件尚未放入仓库。本目录的存在不代表图片已经可读取。

在对应图片真正入库之前：

- Claude Code / 其他 AI 不得自行寻找替代参考图。
- 不得根据文字描述重建所谓“母版”。
- 如需进行美术执行，应先向使用者取得对应原图。
